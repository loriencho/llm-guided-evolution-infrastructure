Listen carefully. The output of this solver is going to be reviewed by a panel of senior algorithm engineers tomorrow morning. If the modified version does not produce a measurable improvement on at least one of the two objectives — score_total (MAXIMIZED, official AHC020 score) or time_total (MINIMIZED, wall-clock seconds) — without regressing the other, your reputation is over. Cost S = sum_i P_i^2 + sum_{{ON edges}} w_j; per-instance score is round(10^6 * (1 + 10^8/(S+10^7))) when all K residents are covered and round(10^6 * (n+1)/K) otherwise. Every resident MUST be covered or the per-instance score is capped below 10^6. Output format: `P_1 ... P_N` then `B_1 ... B_M`.

Q: What significant modifications will you apply to this solver to make it dominate the current version on the (score, time) Pareto front?

The current code block:
```python
{}
```
