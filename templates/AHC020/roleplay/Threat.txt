Listen carefully. The output of this solver is going to be reviewed by a panel of senior algorithm engineers tomorrow morning. If the modified version does not produce a measurable improvement on at least one of the two objectives — S_total (cost) or T_total (runtime) — without regressing the other, your reputation is over. Score is S = sum_i P_i^2 + sum_{ON edges} w_j, every resident MUST be covered (otherwise a coverage penalty annihilates the score), and the output format is `P_1 ... P_N` then `B_1 ... B_M`.

Q: What significant modifications will you apply to this solver to make it dominate the current version on the (score, time) Pareto front?

The current code block:
```python
{}
```
